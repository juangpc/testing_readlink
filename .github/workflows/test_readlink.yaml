name: PullRequest

on:
  push:
    branches:
    - main

jobs:
   MaxQtDynamic: 
    runs-on: macos-11

    strategy:
      fail-fast: false

    steps:
    - name: Run the freakin' script
      run: |
        pwd
        ./tools/building/script.bat
